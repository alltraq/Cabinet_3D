# Shelf configuration
cabinets:
  - cabinet_controller_id: 1234
    # Middle of bottom shelf. Coordinates in feet
    location: (x, y, z)
    zone: "zone_1"
    shelf_height: 1.5
    shelf_width: 2.0
    proximity_threshold: 1.5
  - cabinet_controller_id: 2345
    # Middle of bottom shelf. Coordinates in feet
    location: (x, y, z)
    zone: "zone_1"
    shelf_height: 1.5
    shelf_width: 2.0
    proximity_threshold: 1.5




# LED color codes
led_colors:
  initial: "red"
  rfid_match: "blue"
  rfid_miss: "white"
  off: "off"

# Delay before turning off LED (in seconds)
led_timeout: 5

# Network settings
network:
  geotraqr_address: 20.188.90.124
  cmd_port: 9001
  led_controller_ip: "192.168.1.100"
  led_controller_port: 9100
